export const sqrt3 = Math.sqrt(3);
export const stroke = 5;
export const radius = 2;
export const size = (Math.min(document.documentElement.clientWidth, document.documentElement.clientHeight)*0.35) / (2*radius - 1);
export const animationDuration = 0.2;
